# contains all configuration related to the machines that we might use for the whole infrastructure setup 
service_account_path_server: "/home/window/credentials/"
service_account_path_local: "/home/seang/devops-itp-gen1/ansible-related/lac/task2/credentials/serviceaccount.json"
# adc_file: "/home/kk/Documents/devops_related/devops_itp_gen01/4.ansible-related/IaC/task2/credentials/application_default_credentials.json"
google_project_id: "temporal-bebop-483101-j1"
ssh_pubkey_path: "/home/window/.ssh/id_rsa.pub"
agent_user: "window"

# Define machine spec
machine_specs:
  large:
    machine_type: "e2-standard-2"
    # zone: "asia-southeast1-a"
    zone: "asia-northeast3-a"
    disk_size: 20
    disk_type: "pd-standard" # we have hdd(pd-standard) , ssd( pd-ssd)
  medium:
    machine_type: "e2-medium"
    zone: "asia-northeast3-b"
    disk_size: 20
    disk_type: "pd-standard"

ubuntu_specs:
  v22:
    image: "projects/ubuntu-os-cloud/global/images/family/ubuntu-2204-lts"
  v24:
    image: "projects/ubuntu-os-cloud/global/images/family/ubuntu-2404-lts"

# Define machines that we will create
machines_info:
  # master requirement : RAM 8G, ....
  - name: "master-itp-1"
    machine_type: "{{ machine_specs.large.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machine_specs.large.zone }}"
    disk_size: "{{ machine_specs.large.disk_size }}"
    disk_type: "{{ machine_specs.large.disk_type }}"
  - name: "worker-itp-1"
    machine_type: "{{ machine_specs.medium.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "{{ machine_specs.medium.zone }}"
    disk_size: "{{ machine_specs.medium.disk_size }}"
    disk_type: "{{ machine_specs.medium.disk_type }}"
  - name: "master-itp-2"
    machine_type: "{{ machine_specs.medium.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "asia-east2-a"
    disk_size: "{{ machine_specs.medium.disk_size }}"
    disk_type: "{{ machine_specs.medium.disk_type }}"
  - name: "worker-itp-2"
    image: "{{ ubuntu_specs.v22.image }}"
    machine_type: "{{ machine_specs.medium.machine_type }}"
    #zone: "{{ machine_specs.medium.zone }}"
    zone: "asia-east2-a" # Hongkong instance
    disk_size: "{{ machine_specs.medium.disk_size }}"
    disk_type: "{{ machine_specs.medium.disk_type }}"
  - name: "master-itp-3"
    machine_type: "{{ machine_specs.medium.machine_type }}"
    image: "{{ ubuntu_specs.v22.image }}"
    zone: "asia-east2-a"
    disk_size: "{{ machine_specs.medium.disk_size }}"
    disk_type: "{{ machine_specs.medium.disk_type }}"